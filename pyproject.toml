[project]
name = "bqa"
version = "0.1.0"
description = ""
authors = [
    {name = "Ilia-Luchnikov",email = "luchnikovilya@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy (>=2.3.3,<3.0.0)",
    "multimethod (>=2.0,<3.0)",
    "pytest (>=8.4.2,<9.0.0)"
]

[tool.poetry]
packages = [{include = "bqa", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
